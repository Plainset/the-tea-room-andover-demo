# Pipeline Status

Operational handoff only. `LEADS.md` and `OUTREACH_LOG.md` remain the source of truth.

- Current phase: Build complete, QA passed, independently reviewed 2026-07-15 (verdict PASS, no blocking issues), trivial cosmetic fix applied, deployed.
- Last trusted commit: see `git log` — fix+deploy commit made immediately after this status file update; everything in this repo at that commit is trusted.
- Known untrusted state: none. Working tree is clean at handoff.
- Fixes applied this phase: added explicit `color: var(--ink-soft)` to `.nav-toggle` in `css/styles.css` so the mobile hamburger icon (`stroke="currentColor"`) uses the deliberate brand ink color instead of the browser-default black (cosmetic-only advisory from independent review, not blocking; matches the existing ink-soft color already used elsewhere on cream backgrounds, so no new contrast risk).
- Next exact action: Outreach — draft (never send) the pitch email per AGENTS.md step 7, hook on the Cloudflare 526 error, then update `OUTREACH_LOG.md`. Before actually sending (a separate future step, not this session), do a quick manual (non-curl) re-check of the £1,200 grant date/amount and the owner names (Jackie Lusk & Hayley Rowbotham) once search tools are unblocked — see Flags below. This was NOT done this session (WebSearch/WebFetch confirmed blocked); carry the flag forward.
- Deploy URL: https://plainset.github.io/the-tea-room-andover-demo/ (repo: https://github.com/Plainset/the-tea-room-andover-demo)
- Outreach state: see below (filled in after drafting attempt).
- Independent review (2026-07-15): re-verified the 526 Cloudflare error via fresh `curl` (confirmed on bare/www, http/https), independently re-decoded both Wayback cf-email snapshots in Python (both → `thetearoomandover@gmail.com`, matching phone/address), confirmed the Wayback CDX index has no recoverable images (direct fetch of 3 referenced stock-photo URLs via Wayback's image resource path returned 404, confirming photography is genuinely unrecoverable, not just unused), and re-ran contrast/upscale audits live via local Chrome + puppeteer-core at 390/834/1440px on all 3 pages (0 violations, 0 broken images, matches builder's QA_REPORT). Full evidence table added to QA_REPORT.md under "Independent Reviewer Verification."
- Flags for Alex:
  - The Independent Retailer Grant (May 2025) and the mother-daughter owner names (Jackie Lusk & Hayley Rowbotham) are carried over from the already-vetted LEADS.md entry; this session (and the independent review) could not confirm either fact (Google/Facebook lookups are session-blocked, and Test Valley Borough Council's site search is JS-rendered so it wasn't fetchable via curl). Neither is contradicted by anything found. The site presents both as flat fact with no on-page hedge (consistent with how the rest of the pipeline treats pre-vetted LEADS.md facts) — worth a fast manual check before sending, since a wrong name/figure in unsolicited outreach to the actual owners is more visible than most fact errors.
  - No real photography exists anywhere reachable for this business — confirmed independently (Wayback CDX only ever archived the homepage HTML, and direct fetches of the referenced stock-photo URLs 404). The demo site is deliberately text-forward with hand-drawn inline SVG decoration instead — reviewed and judged to be the correct call, not a corner cut. If Alex ever gets real photos directly from the owners, that would meaningfully upgrade the demo.
  - Minor cosmetic-only note from review: `.nav-toggle` (mobile hamburger button) has no explicit `color` set in CSS, so its icon relies on the browser's default black rather than a deliberate brand color. Renders fine today; not blocking.
